# TechDesk Pro v1.2.0 - Stage 7 Notes

## Decisao

Status: **PASS WITH LIMITATIONS**.

Os gates locais de regressao, migrations, upgrade, preservacao, clean install,
backup, restore-check, seguranca e fluxos E2E passaram. A publicacao das imagens
`1.2.0-rc.1`, seus digests e os smokes com pull real de registry ficam pendentes
porque o workflow oficial exige um commit ja presente em `origin/main` e com CI
verde. Esta Stage cria o commit, mas nao faz push por determinacao da task.

Nao foi criada tag ou release v1.2.0 e a v1.1.1 nao foi alterada.

## Checkpoint inicial

- branch: `main`;
- HEAD inicial: `a2f0f1921b3462d84ecc08e31e8605c157e2ba13`;
- local e `origin/main` sem divergencia no inicio;
- CI remoto: run `32624626133`, sucesso;
- tag v1.1.1: `66746b2d71b8a67dae25f9db66e6bf2d970b55fd`;
- tag e GitHub Release v1.2.0: inexistentes.

## Preparacao de versao

Metadados de package, OpenAPI, frontend, `deploy/VERSION`, Compose, instalador e
exemplos de ambiente foram atualizados para 1.2.0. O pacote RC usa
`1.2.0-rc.1`, inclusive nas referencias de imagem gravadas pelo instalador.

Nenhum schema, migration ou workflow foi alterado nesta Stage.

## Regressao final

Backend:

- `npm ci`: PASS, 0 vulnerabilidades;
- typecheck: PASS;
- testes: 116 PASS e 1 teste condicional ignorado;
- build: PASS;
- audit high: 0 vulnerabilidades.

Frontend:

- `npm ci`: PASS, 0 vulnerabilidades;
- lint: PASS;
- testes: 100/100 PASS em 22 arquivos;
- build: PASS, rotas continuam code-split;
- audit high: 0 vulnerabilidades.

Os 7 arquivos de testes backend incluem as regressoes de setup/deploy,
orcamento misto, estoque confiavel, concorrencia, sessoes e tracking.

## Migration limpa

Um PostgreSQL temporario vazio recebeu as 17 migrations sem falha ou drift.
Foram confirmados:

- `BudgetItemType` com `PART` e `SERVICE`;
- `BudgetItem.type` e `description` obrigatorios;
- `Part.minimumStock` com default 0;
- `User.tokenVersion` com default 0;
- unicidade de `(serviceOrderId, version)`;
- indices de movimentos e relacao de estorno.

O banco temporario foi removido ao final da validacao.

## Baseline e upgrade v1.1.1

Uma instalacao real v1.1.1 foi criada com imagens publicadas no GHCR e banco
representativo contendo ADMIN, RECEPTION, TECHNICIAN, cliente com documento
alfanumerico, equipamento, OS em manutencao, orcamento PART aprovado, peca,
ENTRY, EXIT, REVERSAL, tracking e onboarding concluido.

Backup oficial anterior ao upgrade:

- arquivo: `backup-inicial-producao-2026-08-23_04-35-31.dump`;
- tamanho: 32483 bytes;
- SHA256: `328DD4D4692A23B84C6D06A8F5D40E34D1CC4C272144EBC36BDC1BE17ED9170C`;
- `pg_restore -l`: PASS;
- restore-check isolado: PASS.

O upgrade foi executado pelo comando oficial, incluindo um segundo backup gate,
Compose, migrations, health, ready e metadata. Para o ensaio local, somente o
pull das tags RC ainda inexistentes foi substituido por imagens candidatas
construidas do workspace atual e marcado explicitamente nos logs. Portanto o
fluxo do instalador passou, mas nao conta como upgrade com registry publicado.

Depois do upgrade foram preservados IDs, papeis, hashes de senha, cliente,
documento, equipamento, OS, status, publicToken, orcamento, peca, quantidade,
movimentos, relacao de estorno e configuracoes. O BudgetItem legado passou a
`PART` com descricao `SSD Stage7 Legacy` e preco 250. `minimumStock` e
`tokenVersion` receberam default 0. Credenciais antigas fizeram novo login e um
JWT legado sem a claim `tokenVersion` recebeu 401.

## E2E funcional

- orcamento misto V1: PART snapshot 250 + SERVICE 80, total 330;
- revisao V2: PART snapshot 300 + SERVICE 90, total 390;
- alteracao posterior da peca nao mudou o snapshot V1;
- orcamento SERVICE-only: PASS, sem movimento de estoque;
- impressao de protocolo: HTTP 200;
- impressao de orcamento: HTTP 200;
- estados de estoque OK, LOW_STOCK e OUT_OF_STOCK: PASS;
- duas saidas concorrentes de 4 sobre saldo 6: HTTP 201 e 409, saldo final 2;
- saldo negativo: nao ocorreu;
- historico paginado em duas paginas, sem duplicidade;
- filtros por tipo e periodo: PASS;
- ordenacao newest-first: PASS;
- consumir, estornar e consumir novamente: saldo 10 -> 9 -> 10 -> 9;
- movimento de SERVICE: nenhum;
- approve x reject: HTTP 200 e 409, uma decisao;
- mesma transicao concorrente: HTTP 200 e 409, um historico;
- cancel x action: HTTP 200 e 409, um historico.

## Sessoes e tracking

- dois tokens emitidos antes da troca de senha: ambos 401 depois da troca;
- senha anterior: 401;
- nova senha: login PASS;
- mudanca de papel: token anterior 401;
- papel do tecnico restaurado depois da prova;
- tracking valido: PASS;
- tracking invalido: 404;
- payload publico: somente numero, status, datas e equipamento basico;
- CPF/CNPJ, JWT, password e IDs privados: ausentes;
- frontend usa a origem atual e `/api` same-origin.

## Clean install local

O instalador RC criou uma stack isolada do zero na porta 18083 usando as imagens
candidatas locais explicitamente identificadas:

- 17 migrations: PASS;
- health e ready: PASS;
- frontend: HTTP 200;
- `setupCompleted` inicial false: PASS;
- empresa, RECEPTION e TECHNICIAN criados: PASS;
- `setupCompleted` final true: PASS;
- logout/relogin equivalente por nova sessao: PASS;
- status sem sudo, uid 1000: PASS;
- restart dos tres containers: healthy;
- login e onboarding preservados apos restart: PASS.

Backup da instalacao limpa:

- arquivo: `backup-inicial-producao-2026-08-23_04-57-16.dump`;
- SHA256: `a71721ef42ebfa2f5d61187a6216dbda415ac8591f22dfe5201257100b46915f`;
- `pg_restore -l`: PASS;
- restore-check isolado e relacoes criticas: PASS.

O runtime ficou em volume Windows montado no WSL para permitir espaco e Docker
Desktop. Por isso o modo POSIX real 600 de `.env` nao pode ser comprovado nesse
runtime DrvFS. Os testes automatizados do instalador validam a politica 600; a
confirmacao em `/opt/techdesk-pro` fica para o smoke Linux.

## Seguranca de producao

- PostgreSQL sem porta publicada: PASS (`5432/tcp` interno somente);
- Swagger em producao: 404;
- CORS permitido para a origem configurada e sem wildcard/reflexao indevida;
- origem externa nao recebeu `Access-Control-Allow-Origin`;
- metadata sem campos de secret;
- logs de setup, upgrade, backup, API e frontend: zero ocorrencias dos valores
  reais de senha administrativa, JWT secret, senha PostgreSQL, senha da
  DATABASE_URL ou cabecalho Bearer;
- imagens locais: metadata sem secret;
- tracking privacy: PASS.

## Imagens e artefato RC

Imagens candidatas locais, nao publicadas:

- `ghcr.io/welissonhrq21/techdesk-pro-api:1.2.0-rc.1`;
- `ghcr.io/welissonhrq21/techdesk-pro-frontend:1.2.0-rc.1`.

Os IDs locais nao sao RepoDigests e nao sao registrados como digests oficiais.
O conteudo local confirmou 17 migrations, OpenAPI 1.2.0, minimumStock,
tokenVersion, frontend code-split, QR, `/api` same-origin e ausencia de
`localhost:3333` e secrets.

Artefato:

- `dist/techdesk-pro-setup-1.2.0-rc.1.tar.gz`;
- tamanho: 26921 bytes;
- SHA256: `5B5BF8C48D69661937BB2CA5F8AAF148BF0D4655443EDDC1B9188EB09439CD6B`;
- gzip/tar e 29 entradas: PASS;
- modos do pacote: executaveis 755 e arquivos publicos 644;
- sem `.env`, logs, backups, banco, `node_modules`, `.git` ou secrets.

## Limitacoes obrigatorias

1. As imagens `1.2.0-rc.1` nao foram publicadas no GHCR.
2. API e frontend RepoDigests oficiais ainda nao existem.
3. Clean install e upgrade com `docker pull` real dessas tags nao foram feitos.
4. Reboot real e autostart do host nao foram testados.
5. Permissao 600 em `/opt/techdesk-pro/.env` requer o smoke Linux nativo.

O workflow `.github/workflows/release-images.yml` exige SHA completo ja presente
no repositorio remoto e aprovado pelo CI. Publicar antes do commit e do CI
quebraria a rastreabilidade hardened. Nenhum push improvisado, tag final ou
`docker push` manual foi usado.

## Proximo gate

Depois do checkpoint do commit desta Stage:

1. aguardar CI verde do commit exato;
2. disparar `Build Release Candidate Images` com `1.2.0-rc.1` e o SHA aprovado;
3. registrar os dois RepoDigests e OCI revision;
4. repetir clean install e upgrade v1.1.1 usando pull real do GHCR;
5. executar reboot/autostart e conferir modo 600 em Ubuntu Server;
6. somente entao promover imagens e criar tag/release final v1.2.0.

Recomendacao atual: **READY WITH LIMITATIONS**, pronto para checkpoint e smoke
Linux final depois da publicacao das imagens RC.
