# TechDesk Pro v1.2.1-rc.1 acceptance notes

This candidate validates the v1.2.1 patch scope before any final tag, GitHub
Release, or image publication.

## Candidate identity

- Application/OpenAPI identity: `1.2.1-rc.1`
- Setup artifact: `techdesk-pro-setup-1.2.1-rc.1.tar.gz`
- Runtime image references: exact `1.2.1-rc.1` tags
- Installer and installed metadata: `1.2.1-rc.1`
- Final `1.2.1` references are not mixed into the operational RC artifact.

The source deployment baseline remains `1.2.1`; deterministic packaging
materializes the exact candidate identity without editing the source tree.

## Required acceptance

- Complete backend, frontend, setup, security, JSON-purity, and LF regression.
- Reproducible package checks before and after acceptance.
- Clean Ubuntu Server 24.04 installation with real Docker and systemd.
- Upgrade from the official v1.2.0 release artifact with data preservation.
- Real backup, validation, restore-check, restart, and repair operations.
- Real VM reboot followed by contract and operation checks.
- TechDesk Manager adapter/validator execution against the installed candidate.

For `1.2.1-rc.1`, Manager compatibility must be
`VALIDATION_CANDIDATE`, not `SUPPORTED_RELEASE`. Mutation is allowed only when
the exact candidate version, schema `1.0`, and all eight required capabilities
are simultaneously validated. Otherwise the mutation gate must block.

No manual runtime injection is allowed. Local unpublished candidate images may
be used only as pre-publication laboratory artifacts with their exact candidate
tags and recorded provenance.
