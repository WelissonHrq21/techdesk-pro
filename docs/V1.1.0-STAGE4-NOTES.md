# TechDesk Pro v1.1.0 - Stage 4 Notes

## Scope

Stage 4 adds optional CPF/CNPJ identification to `Customer`.

Included:

- Nullable `Customer.document`.
- Unique normalized document when present.
- CPF validation with verifier digits.
- Numeric and alphanumeric CNPJ validation with verifier digits.
- Customer search by normalized or formatted document.
- Backend minimization by role.
- Frontend form, mask, validation, detail display, and search support.
- OpenAPI update.
- Upgrade migration validation.

Not included:

- Onboarding.
- Installer/setup experience.
- `AppSettings`.
- JWT changes.
- Code splitting.
- New print exposure.

## Migration

Migration:

```text
prisma/migrations/20260817035000_add_customer_document/migration.sql
```

Changes:

```sql
ALTER TABLE "Customer" ADD COLUMN "document" TEXT;
CREATE UNIQUE INDEX "Customer_document_key" ON "Customer"("document");
```

The column is nullable. Existing customers keep `document = null`.

PostgreSQL unique indexes allow multiple `NULL` values, so existing and future customers without document can coexist.

## Normalization

Input is normalized by the backend before persistence.

Examples:

```text
529.982.247-25 -> 52998224725
11.222.333/0001-81 -> 11222333000181
12.abc.345/01de-35 -> 12ABC34501DE35
```

Contract:

- Field absent on update: does not change the document.
- `document: null`: removes the document.
- `document: ""` or whitespace: removes the document.
- CPF stores digits only.
- CNPJ stores a canonical uppercase numeric or alphanumeric value without punctuation or spaces.

## Validation

Validation is centralized in:

```text
src/utils/customerDocument.ts
```

CPF:

- Must have 11 digits.
- Must pass verifier-digit calculation.
- Repeated digit sequences are rejected.

CNPJ:

- Must have 14 positions.
- The first 12 positions may contain digits `0-9` or uppercase letters `A-Z`.
- The last 2 positions must be numeric verifier digits.
- Must pass Receita Federal modulo 11 verifier-digit calculation.
- Character values use the official ASCII value minus 48 conversion.
- Traditional numeric CNPJ remains valid.
- Repeated base sequences are rejected.

## CNPJ Alphanumeric

The Receita Federal CNPJ alphanumeric format is already part of the 2026 transition for new registrations.

Stage 4.1 adds support for the Receita Federal alphanumeric CNPJ format while preserving traditional numeric CNPJ compatibility.

Reference used during implementation:

```text
https://www.gov.br/receitafederal/pt-br/centrais-de-conteudo/publicacoes/perguntas-e-respostas/cnpj/cnpj-alfanumerico.pdf
```

## Uniqueness

Uniqueness applies to the normalized value.

Examples:

```text
529.982.247-25
52998224725
12.abc.345/01de-35
12ABC34501DE35
```

Masked/unmasked and lowercase/uppercase variants are treated as the same document. Duplicate create/update attempts return `409` with:

```text
Customer document already exists
```

Prisma `P2002` errors are translated to domain errors for both `document` and existing `email` uniqueness.

## Search

`GET /customers?search=...` now searches by:

- name;
- phone;
- email;
- normalized document when the search term contains document characters.

Formatted input works because the search term is normalized before adding the document condition.

## RBAC And Privacy

`ADMIN`:

- Can create/edit document.
- Can view document.
- Can search by document.

`RECEPTION`:

- Can create/edit document.
- Can view document.
- Can search by document.

`TECHNICIAN`:

- Cannot create/edit customers by existing RBAC.
- Can search customers for technical flows, but `document` is omitted from response payloads.
- Can open service-order detail, but `serviceOrder.customer.document` is omitted.

Minimization is centralized through:

```text
src/serializers/customerSerializer.ts
```

## Public Tracking

`GET /public/service-orders/:token` still does not return `Customer`.

Regression test confirms the response does not contain the document value.

## Prints

Stage 4 does not add CPF/CNPJ to:

- OS receipt print.
- Budget print.

Decision: do not expand exposure of personal data in printed documents until there is an explicit operational requirement.

## Logs

Logger redaction now includes:

- `req.body.document`.
- `request.body.document`.
- generic `document`.

This prevents CPF/CNPJ from being emitted if request body serialization is enabled.

## Frontend

Customer form:

- Adds optional `CPF/CNPJ`.
- Applies visual CPF/CNPJ mask, including alphanumeric CNPJ.
- Validates CPF/CNPJ before submit.
- Sends normalized canonical value or `null`.

Customer detail:

- Shows formatted document when the backend sends it.
- Does not show the document block when the property is omitted.

Customer search:

- Placeholder now includes CPF/CNPJ.
- Search input is sent to the backend; backend remains responsible for normalization.

New service order:

- Customer search supports CPF/CNPJ.
- Document is shown only when present in the authorized backend response.

## Upgrade Validation

Manual isolated upgrade validation:

- Applied all previous migrations except Stage 4.
- Inserted legacy customers without document.
- Applied Stage 4 migration.
- Confirmed legacy customers stayed with `document = null`.
- Confirmed multiple `NULL` documents are allowed.
- Confirmed a new customer with document can be inserted.
- Confirmed duplicate document is blocked by `Customer_document_key`.

## Tests

Backend:

- CPF valid.
- CPF invalid.
- CPF with mask normalized.
- Repeated CPF invalid.
- Numeric CNPJ valid.
- Numeric CNPJ invalid.
- Numeric CNPJ with mask normalized.
- Alphanumeric CNPJ valid.
- Alphanumeric CNPJ lowercase normalized to uppercase.
- Alphanumeric CNPJ with mask normalized.
- Alphanumeric CNPJ invalid verifier digit rejected.
- Alphanumeric CNPJ invalid length rejected.
- Alphanumeric CNPJ forbidden character rejected.
- Missing document accepted.
- `document: null` accepted.
- Empty document normalized to null.
- Duplicate POST blocked across masked/unmasked and lowercase/uppercase values.
- Duplicate UPDATE blocked.
- Updating with same own document allowed.
- Removing document allowed.
- Search by normalized/formatted/lowercase/uppercase document.
- ADMIN receives document.
- RECEPTION receives document.
- TECHNICIAN does not receive document.
- Service-order detail omits document for TECHNICIAN.
- Public tracking omits document.

Frontend:

- Form accepts no document.
- Valid CPF.
- Invalid CPF.
- Valid numeric CNPJ.
- Valid alphanumeric CNPJ.
- Invalid CNPJ.
- Numeric and alphanumeric mask/formatting.
- Friendly 409.
- Edit adds document.
- Edit replaces document with alphanumeric CNPJ.
- Edit removes document with `null`.
- Search by numeric and alphanumeric document.
- ADMIN detail displays document.
- ADMIN detail displays alphanumeric CNPJ.
- RECEPTION detail displays document.
- TECHNICIAN detail omits document.
- Existing customer form flow preserved.

## Validation Results

- Backend typecheck: PASS.
- Backend tests: PASS, 45 tests.
- Backend build: PASS.
- Frontend lint: PASS.
- Frontend tests: PASS, 44 tests.
- Frontend build: PASS.
- Stage 3/3.1 stock reversal regression remains covered by backend and frontend suites.

## Result

Stage 4 plus Stage 4.1 are approved from implementation, privacy, migration, and automated regression perspectives.
