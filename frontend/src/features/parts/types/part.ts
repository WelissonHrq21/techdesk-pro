export type Part = {
  id: string;
  name: string;
  brand: string;
  currentPrice: string;
  stock: number;
  supplier: string | null;
  active: boolean;
  createdAt: string;
  updatedAt: string;
};
